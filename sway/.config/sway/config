font "JetBrainsMono NF" 12px

output * bg $HOME/.config/sway/wallpaper.jpg fill

bindsym Mod1+q kill
bindsym Mod1+Shift+e exit
bindsym Mod1+Shift+r reload

bindsym Mod1+s splitv
bindsym Mod1+Shift+s splith

bindsym Mod1+h focus left
bindsym Mod1+j focus down
bindsym Mod1+k focus up
bindsym Mod1+l focus right

bindsym Mod1+Shift+h move left
bindsym Mod1+Shift+j move down
bindsym Mod1+Shift+k move up
bindsym Mod1+Shift+l move right

bindsym Mod1+1 workspace number 1
bindsym Mod1+2 workspace number 2
bindsym Mod1+3 workspace number 3
bindsym Mod1+4 workspace number 4
bindsym Mod1+5 workspace number 5

bindsym Mod1+Shift+1 move container to workspace number 1
bindsym Mod1+Shift+2 move container to workspace number 2
bindsym Mod1+Shift+3 move container to workspace number 3
bindsym Mod1+Shift+4 move container to workspace number 4
bindsym Mod1+Shift+5 move container to workspace number 5

bindsym Mod1+Return exec alacritty
bindsym Mod1+Space exec $HOME/.config/sway/launcher.sh

bindsym Print exec sh -c 'slurp | grim -g - - | wl-copy'
bindsym Mod1+Print exec sh -c 'slurp | grim -g - ~/screenshots/screenshot_$(date +%Y-%m-%d_%H%M%S).png'

bindsym --locked XF86AudioMute exec wpctl set-mute @DEFAULT_SINK@ toggle
bindsym --locked XF86AudioMicMute exec wpctl set-mute @DEFAULT_SOURCE@ toggle
bindsym --locked XF86AudioRaiseVolume exec wpctl set-volume @DEFAULT_SINK@ 5%+ --limit 1
bindsym --locked XF86AudioLowerVolume exec wpctl set-volume @DEFAULT_SINK@ 5%- --limit 1

bindsym --locked XF86MonBrightnessUp exec brightnessctl set 5%+
bindsym --locked XF86MonBrightnessDown exec brightnessctl set 5%-

bindgesture swipe:left workspace next
bindgesture swipe:right workspace prev

bindsym Mod1+Shift+minus move scratchpad
bindsym Mod1+minus scratchpad show

mode "resize" {

    bindsym h resize shrink width 10px
    bindsym j resize grow height 10px
    bindsym k resize shrink height 10px
    bindsym l resize grow width 10px

    bindsym Escape mode "default"
}

bindsym Mod1+r mode "resize"

bindsym Mod1+f fullscreen
bindsym Mod1+Shift+Space floating toggle

default_border none 0
default_floating_border none 0

default_dim_inactive 0.2

for_window {
    [app_id="launcher"] floating enable
    [title="(?:Open|Save) (?:File|Folder|As)"] floating enable
}

input type:touchpad {
    tap enabled
    natural_scroll enabled
}

input type:keyboard {
    repeat_rate 25
    repeat_delay 250
}

bar {
    output eDP-1
    position top
    status_command $HOME/.config/sway/status.sh
    tray_output none

    colors {
        background #111111E6
    	focused_workspace #DDDDDD #DDDDDD #111111
    	active_workspace #DDDDDDE6 #DDDDDDE6 #111111
    	inactive_workspace #111111E6 #111111E6 #DDDDDD
    }
}

include /etc/sway/config.d/*
